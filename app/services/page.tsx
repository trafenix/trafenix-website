import type { Metadata } from "next"; import { SiteHeader } from "@/components/site-header"; import { SiteFooter } from "@/components/site-footer"; import { ServiceCard } from "@/components/service-card"; import { services } from "@/lib/services";
export const metadata:Metadata={title:"13 Traffic Survey Services",description:"Explore 13 professional traffic survey and video-analysis services from Trafenix."};
export default function Services(){return <><SiteHeader/><main><section className="page-hero"><div className="shell"><span className="eyebrow">Professional traffic surveys</span><h1>Thirteen ways to turn footage into answers.</h1><p>Choose a specialist analysis or combine services into one coordinated project. Every delivery is scoped, quality-checked and formatted for practical use.</p></div></section><section className="section"><div className="shell"><div className="service-grid">{services.map(s=><ServiceCard service={s} key={s.slug}/>)}</div></div></section><section className="cta-band"><div className="shell cta-inner"><div><h2>Not sure which service fits?</h2><p>Share the project goal and a sample clip. We’ll recommend the right scope.</p></div><a className="button light-button" href="/contact">Ask Trafenix</a></div></section></main><SiteFooter/></>}
