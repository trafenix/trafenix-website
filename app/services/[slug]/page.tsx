import type { Metadata } from "next"; import { notFound } from "next/navigation"; import { ArrowLeft, ArrowRight } from "lucide-react"; import { SiteHeader } from "@/components/site-header"; import { SiteFooter } from "@/components/site-footer"; import { services } from "@/lib/services";
export function generateStaticParams(){return services.map(s=>({slug:s.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const s=services.find(x=>x.slug===slug);return s?{title:s.title,description:s.summary}:{} }
export default async function ServiceDetail({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const service=services.find(s=>s.slug===slug);if(!service)notFound();return <><SiteHeader/><main><section className="page-hero"><div className="shell"><a href="/services" className="text-link back-link"><ArrowLeft size={16}/> All services</a><span className="eyebrow block">Service {service.number} · {service.category}</span><h1>{service.title}</h1><p>{service.summary}</p></div></section><section className="content-section"><div className="shell content-grid"><div><h2>What you receive</h2><p>We agree the movements, classes, intervals and validation approach before processing begins. Your delivery is structured for review and downstream use.</p><ul className="detail-list">{service.deliverables.map(x=><li key={x}>{x}</li>)}<li>Methodology, assumptions and quality notes</li><li>Excel or CSV data with a PDF summary where agreed</li></ul></div><aside className="info-panel"><h3>Common applications</h3><div className="chip-row">{service.useCases.map(x=><span className="chip" key={x}>{x}</span>)}</div><h3 className="spaced-title">Standard workflow</h3><p>Footage review → scope confirmation → AI/manual processing → validation → delivery.</p><a className="button" href={`/contact?service=${service.slug}`}>Request this service <ArrowRight size={17}/></a></aside></div></section></main><SiteFooter/></>}
