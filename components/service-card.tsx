import { ArrowUpRight, Bike, Car, ChartNoAxesCombined, CircleParking, Gauge, Layers3, ListTree, MapPinned, Route, ShieldAlert, SignpostBig, TrafficCone, Waypoints } from "lucide-react"; import type { Service } from "@/lib/services";
const icons: Record<string, React.ComponentType<{size?:number}>> = { Route, Car, Waypoints, Chart: ChartNoAxesCombined, List: ListTree, Bike, Map: MapPinned, Parking: CircleParking, Shield: ShieldAlert, Ban: SignpostBig, Traffic: TrafficCone, Gauge, Layers: Layers3 };
export function ServiceCard({service}: {service: Service}) { const Icon=icons[service.icon]||Route; return <a href={`/services/${service.slug}`} className="service-card" aria-label={`View ${service.title}`}><div className="card-top"><span className="service-number">{service.number}</span><span className="icon-box"><Icon size={22}/></span></div><span className="category">{service.category}</span><h3>{service.title}</h3><p>{service.summary}</p><ul>{service.deliverables.map(x=><li key={x}>{x}</li>)}</ul><span className="text-link">View service <ArrowUpRight size={16}/></span></a> }
